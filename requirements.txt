# 버전은 하한만 지정한다. 폐쇄망에서는 사내 미러에 있는 버전이
# 제각각이라 상한을 박아 두면 설치 자체가 막힌다.

# MCP — 스펙 리비전 2026-07-28 (stateless 코어, MRTR, 확장 프레임워크).
# 2.0 미만은 2025-11-25 까지만 지원하므로 하한을 2.1 로 둔다.
mcp>=2.1

# LLM / 그래프
langchain>=1.0
langgraph>=0.2
langchain-ollama>=0.2

# 웹 (챗봇 UI)
flask>=3.0

# HTTP (Ollama 헬스체크). langchain-ollama 의 의존성이지만 명시해 둔다.
httpx>=0.27

# 테스트
pytest>=8.0

# Outlook 메일을 읽는 에이전트(iferr)를 쓸 때만 필요하다.
# Windows + Outlook 데스크톱이 설치된 PC 에서만 설치된다.
# MAIL_BACKEND=eml 로는 이것 없이도 동작한다.
# pywin32>=306

# Oracle 을 쓰는 에이전트가 있을 때만 필요하다.
# 없어도 나머지 기능은 동작한다. 필요해지면 주석을 풀 것.
# 기본은 thin 모드라 클라이언트 설치가 필요 없지만, 오래된 서버(대개 12.1
# 미만)는 DPY-3010 으로 거절한다. 그때는 Oracle Client 가 설치된 PC 에서
# thick 모드로 자동 전환된다(config.ORACLE_THICK_MODE 참고).
# oracledb>=2.0

# langchain-mcp-adapters 는 쓰지 않는다: mcp<2.0 을 핀하고 있어
# 2026-07-28 리비전과 함께 설치할 수 없다. MCP 툴 → LangChain 툴 변환은
# app/mcp_bridge.py 에서 직접 한다.
