CREATE OR REPLACE PROCEDURE ORD_BATCH IS
  -- IF_ORDER_TMP 를 읽어 ORD_HDR 로 적재한다 (주석; 사용처 아님)
BEGIN
  INSERT INTO ORD_HDR (ORD_NO, TOTAL_AMT)
  SELECT ORD_NO, TOTAL_AMT
    FROM IF_ORDER_TMP
   WHERE STATUS = 'N';

  UPDATE IF_ORDER_TMP
     SET STATUS = 'Y'
   WHERE STATUS = 'N';
END;
