<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="order">

  <!-- 주문 목록 조회 (IF_ORDER_TMP 언급은 주석이라 사용처가 아니다) -->
  <select id="selectOrderList" resultType="map">
    SELECT ORD_NO, TOTAL_AMT
      FROM IF_ORDER_TMP
     WHERE STATUS = #{status}
  </select>

  <update id="updateOrderStatus">
    UPDATE IF_ORDER_TMP
       SET STATUS = #{status}
     WHERE ORD_NO = #{ordNo}
  </update>

</mapper>
